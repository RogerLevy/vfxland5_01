\ ------------------------------------------------------------------------------ 
\ sprite example
\ ------------------------------------------------------------------------------ 
 
\ Constants
\ 320 constant gamew
\ 240 constant gameh
\ 1024 constant #actors

\ Load engine
include %vfxland5%\supershow\supershow.vfx

\ load game-specific modules (dependencies)
\ include %idir%\...

\ Load autoclasses, scripts, scenes, and modes
load-project

redef : main
    just
    2 for 50 50 at %spewer one drop loop
;

\ Load tools and run tests
debug @ [if]
    main
[then]

