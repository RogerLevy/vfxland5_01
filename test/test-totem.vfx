include %idir%/../engineer/nib2-serdes.vfx
include %idir%/../engineer/totem.vfx

class: abc 
    is-a _totem 
    prop a <save 
    prop b <save 
    prop c <save 
    :: default-path ( - path len ) f" test/abc" ;
class;

\ abc +totem test 
\ [[  1 a ! 2 b ! 3 c !
\ me ]] manifest
\ test.abc commit

\ test.abc [[ 0 a ! 0 b ! 0 c ! ]]
\ s" test.abc" abc load-totem [[ a ? b ? c ? ]]
s" abc" abc reheat-dir
test.abc [[ a ? b ? c ? ]]
test2.abc [[ a ? b ? c ? ]]