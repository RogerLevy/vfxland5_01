empty

class: abc 
    is-a _totem 
    prop a <save 
    prop b <save 
    prop c <save 
    default-dir: abc
    extension: abc
    version: 1
class;

\ abc +totem test 
\ [[  1 a ! 2 b ! 3 c !
\ me ]] manifest
\ test.abc commit

\ test.abc [[ 0 a ! 0 b ! 0 c ! ]]
\ s" test.abc" abc load-totem [[ a ? b ? c ? ]]

s" abc" abc load-default-dir
test.abc [[ a ? b ? c ?  999 b ! ]]
test2.abc [[ a ? b ? c ? ]]

test.abc keep