trait: %object
    :: init ( - ) ;
    :: deinit ( - ) ;
    \ :: think ( - ) ;
trait;

trait: %loadable
    :: load ( - ) ;
trait;

trait: %unloadable
    :: unload ( - ) ;
trait;

: ?unload  ?dup if unload then ;

trait: %usable
    :: use ( - ) ;
trait;

trait: %drawable
    :: draw ( - ) ;
trait;